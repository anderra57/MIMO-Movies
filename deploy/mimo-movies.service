[Unit]
Description="MIMO Movies App"

[Service]
ExecStart=/usr/bin/docker run -p 80:3000 anderpri/mimo-movies:app-amd64
ExecStop=/usr/bin/docker stop $(/usr/bin/docker ps -q --filter ancestor=anderpri/mimo-movies:app-amd64)
Type=simple
Restart=always

[Install]
WantedBy=multi-user.target